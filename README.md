# Renewables Capture Prices & Cannibalization (DE/LU)

This project builds a clean, reproducible dataset for German day-ahead power prices and renewable generation signals (wind/solar) plus load.
Goal: compute capture prices/ratios and visualize cannibalization effects with a simple, auditable pipeline.
Start small: ingest → clean → merge → sanity checks → first plot.
